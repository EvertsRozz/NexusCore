# NexusCore Electron app

# How to build

The configuration contains multiple scripts for each build process, however there is a one and done script

```console
yarn bnr
```

(shorthand for build and run)
